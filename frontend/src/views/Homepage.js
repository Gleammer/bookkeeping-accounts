import { Container, Row, Col } from "react-bootstrap";

const Homepage = () => {
    return (<Container>
        <Row>
            <Col>
                <h1>This is Homepage!</h1>
            </Col>
        </Row>
    </Container>);
};

export default Homepage;
